/* Auto-generated by Specappular — do not edit directly */
export class <%= context.className || spec.className || spec.name %> {
<% (context.fields || spec.fields || []).forEach(f => { -%>
  <%= f.name %>: <%= f.type %>;
<% }); -%>

  constructor(init?: Partial<<%= context.className || spec.className || spec.name %>>) {
    Object.assign(this, init);
  }
}